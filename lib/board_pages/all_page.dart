
/*import 'dart:js';

import 'package:firebase_auth/firebase_auth.dart';
import 'package:flutter/material.dart';
import 'package:modu/board_pages/add_list.dart';
import 'package:page_transition/page_transition.dart';

class AllPage extends StatelessWidget {
  final User user;

  AllPage(this.user);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SingleChildScrollView(
        child: Column(
          children :<Widget>[
            _buildList(),
            _buildButton(),
          ],
        )
      ),
    );
  }
}

Widget _buildList(){
  return SafeArea(child: child);
}

Widget _buildButton(){
  return SafeArea(
      child: Container( //리스트 추가하기 버튼
        margin: EdgeInsets.fromLTRB(0, 5, 10, 70),
        child:Align(
          alignment: Alignment.bottomRight,
          child:FloatingActionButton.extended(
            //onPressed: () => flutterDialog(),
            onPressed: () => ,
            //onPressed: () => Navigator.push(context, PageTransition(child:TodoListPage(), type: PageTransitionType.fade )),
            backgroundColor: Colors.teal[300],
            label: Text('ADD',style: TextStyle(fontSize: 17),),
            icon: Icon(Icons.add,size: 20,),
          ),
        ),
      ),
  );
}*/